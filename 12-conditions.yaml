- name: Conditions in ansible #Findout whether the taks should run or not to run
  hosts: local
  connection: local
  vars:
    IS_LIVE: true
  tasks:
    - name: Check live or not
      ansible.builtin.debug:
        msg: "Yes, It is live"
      when: IS_LIVE
