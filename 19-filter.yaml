- name: Demo on filters
  hosts: local
  connection: local
  vars:
    topics: linux, shell, ansible, terraform #String datatype
  tasks:
    - name: Greeting message
      ansible.builtin.debug:
        msg: "HI, Kesava {{ GREETINGS | default('World') }}"

    - name: Converting String to list
      ansible.builtin.debug:
        msg: " {{ topics | split(',') }} "
